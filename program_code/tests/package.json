{
  "name": "answerbetter-sr",
  "version": "1.0.0",
  "main": "../../server.js",
  "scripts": {
    "test": "jest --config jest.config.js --runInBand",
    "coverage": "jest --config jest.config.js --coverage --runInBand"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}